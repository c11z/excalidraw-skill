# Diagrams

Diagramming projects created with the `excalidraw-diagram` skill (`.claude/skills/excalidraw-diagram/`).

Suggested layout: one subdirectory per project, containing the `.excalidraw` source files and their rendered `.png` outputs.
